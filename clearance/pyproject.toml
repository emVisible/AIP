[project]
name = "clearance"
version = "0.1.0"
description = "Clearance（放行）：Laya-native 通用后台审核网关，AIP 的产品实现"
requires-python = ">=3.10"

[project.optional-dependencies]
laya = ["laya"]
server = ["fastapi", "uvicorn"]

[tool.pytest.ini_options]
testpaths = ["tests"]
