# CHANGELOG — Clearance

## v0.1.0
- 通用审核网关骨架：submit → 决策 → 自动过/拦/转人工 → 人审闭环。
- Laya sidecar 独立进程（torch 依赖隔离），缺席诚实降级 heuristic。
- 前端（pnpm + Vite + React + TS）+ `./start.sh` 一键三进程。
- eval 阈值 sweep 工具链；AIP 思想折叠为 `PROTOCOL.md` 一页表。
